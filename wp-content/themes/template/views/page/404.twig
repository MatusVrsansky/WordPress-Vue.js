{% extends "layout.twig" %}

{% block content %}

  <div class="container mt-4">
    <div class="white-wrapper text-center">
      <h1>{{ getOption('theme_settings__404_page__title') }}</h1>
      {{ getOption('theme_settings__404_page__text') }}
      <a href="/">{{function('get_home_url')}}</a>
    </div>
  </div>

{% endblock %}
