<div class="container">
  {% if fields.table == 'tic-tac-toe' %}
    <h2 class="text-center m-3">Skóre Piškôrky</h2>
    {% set pagination = function('paginationHighScore', 'top_players_tic_tac_toe') %}
    {{ pagination }}
  {% else %}
    <h2 class="text-center m-3">Skóre Pexeso</h2>
    {% set pagination = function('paginationHighScore', 'top_players_memory_game') %}
    {{ pagination }}
  {% endif %}
</div>
